tailwind.config = {
  theme: {
    extend: {
      colors: {
        'primaria': 'var(--cor-primaria)',
        'primaria-hover': 'var(--cor-primaria-hover)',
        'texto-principal': 'var(--cor-texto-principal)',
        'texto-secundario': 'var(--cor-texto-secundario)',
        'texto-sutil': 'var(--cor-texto-sutil)',
        'texto-contraste': 'var(--cor-texto-contraste)',
        'fundo-escuro': 'var(--cor-fundo-escuro)',
        'fundo-claro': 'var(--cor-fundo-claro)',
        'fundo-medio': 'var(--cor-fundo-medio)',
        'borda': 'var(--cor-borda)',
        'erro': 'var(--cor-erro)',
        'sucesso': 'var(--cor-sucesso)',
        'fundo-header': 'var(--cor-fundo-header)',
      }
    }
  }
}